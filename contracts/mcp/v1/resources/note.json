{
  "contract_version": 1,
  "description": "Read one memory note by immutable identifier with lifecycle and source metadata.",
  "mimeType": "application/json",
  "name": "note",
  "responseSchema": {
    "$ref": "schemas/success-envelope.json"
  },
  "uriTemplate": "memory://v1/note/{id}"
}
