# Contributing

Read the implementation plan and frozen MCP V1 contract before making changes. Implement phases in order, write tests before implementation, and do not weaken tests to pass a gate.

Run `make check` before submitting changes. Contract changes additionally require a compatibility review and an ADR; breaking V1 changes require parallel V2 capabilities.
